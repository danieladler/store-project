<div class="welcome">
  <% if @current_user %>
    Hello, <span class="greeting"><%=@current_user.email%></span>. Here is your cart:</h1>
  <% end %>
</div>

  <% @user_orders.each do |o| %>
    <div class="order-block">
      <strong>order name:</strong><%= o.name %>
      <br>
      <strong>total amount: $</strong><%= o.total %>
      <br>
      <strong>last modified:</strong> <%= o.updated_at %>
    </div>
  <% end %>

  <br>

<span class="checkout-link"> <%= link_to "Checkout" , checkout_path %></span>
